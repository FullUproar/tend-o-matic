# @tend-o-matic/compliance

The kernel. Pure TypeScript, no I/O: dated immutable rulesets, tax engine, limit engine, receipt content rules, promo validation. Property-tested and fuzz-tested. The cart cannot bypass it.
